# Event-Storming-Component

It generates our event storming diagram by using data from our event-driven backends.

We can track the system design for thosse backend that has already implemented `commandInvoker`. For example, `ecapi3` and the `web.sales` module.

