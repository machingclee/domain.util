package com.machingclee.domain.util;

// This is a library module — no main class or @SpringBootApplication needed.
// Beans are registered via DomainUtilAutoConfiguration (Spring Boot auto-configuration).
